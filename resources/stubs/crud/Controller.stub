<?php

namespace {{controllerNamespace}};

use App\Http\Controllers\Controller;
use {{modelNamespace}}\{{modelName}};
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Request;
use {{requestNamespace}}\{{modelName}}Request;
use Illuminate\Support\Facades\Redirect;
use Illuminate\View\View;

class {{modelName}}Controller extends Controller
{
     /**
     * Constructor.
     *
     * @return \Illuminate\Http\Response
     */
    function __construct()
    {
        $this->middleware('permission:{{modelRoute}}-list',  ['only' => ['index']]);
        $this->middleware('permission:{{modelRoute}}-view',  ['only' => ['show']]);
        $this->middleware('permission:{{modelRoute}}-create',['only' => ['create','store']]);
        $this->middleware('permission:{{modelRoute}}-edit',  ['only' => ['edit','update']]);
        $this->middleware('permission:{{modelRoute}}-delete',['only' => ['destroy']]);
    }
    /**
     * Display a listing of the resource.
     */
    public function index(Request $request): View
    {
        $pagination_mode = $request->input('pagination_mode','client');
        if($pagination_mode == 'client'){
            ${{modelNamePluralSnakeLowerCase}} = {{modelName}}::all();
            return view('{{modelViewFolder}}.{{modelView}}.index', compact('{{modelNamePluralSnakeLowerCase}}'));
        }
        ${{modelNamePluralSnakeLowerCase}} = {{modelName}}::paginate();

        return view('{{modelViewFolder}}.{{modelView}}.index', compact('{{modelNamePluralSnakeLowerCase}}'))
            ->with('i', ($request->input('page', 1) - 1) * ${{modelNamePluralSnakeLowerCase}}->perPage());
    }

    /**
     * Show the form for creating a new resource.
     */
    public function create(): View
    {
        ${{modelNameSingularSnakeLowerCase}} = new {{modelName}}();

        return view('{{modelViewFolder}}.{{modelView}}.create', compact('{{modelNameSingularSnakeLowerCase}}'));
    }

    /**
     * Store a newly created resource in storage.
     */
    public function store({{modelName}}Request $request): RedirectResponse
    {
        {{modelName}}::create($request->validated());

        return Redirect::route('{{modelRoute}}.index')
            ->with('success', '{{modelName}} created successfully.');
    }

    /**
     * Display the specified resource.
     */
    public function show($id): View
    {
        ${{modelNameSingularSnakeLowerCase}} = {{modelName}}::find($id);

        return view('{{modelViewFolder}}.{{modelView}}.show', compact('{{modelNameSingularSnakeLowerCase}}'));
    }

    /**
     * Show the form for editing the specified resource.
     */
    public function edit($id): View
    {
        ${{modelNameSingularSnakeLowerCase}} = {{modelName}}::find($id);

        return view('{{modelViewFolder}}.{{modelView}}.edit', compact('{{modelNameSingularSnakeLowerCase}}'));
    }

    /**
     * Update the specified resource in storage.
     */
    public function update({{modelName}}Request $request, {{modelName}} ${{modelNameSingularSnakeLowerCase}}): RedirectResponse
    {
        ${{modelNameSingularSnakeLowerCase}}->update($request->validated());

        return Redirect::route('{{modelRoute}}.index')
            ->with('success', '{{modelName}} updated successfully');
    }

    public function destroy($id): RedirectResponse
    {
        {{modelName}}::find($id)->delete();

        return Redirect::route('{{modelRoute}}.index')
            ->with('success', '{{modelName}} deleted successfully');
    }
}
