<form action="{{ route('{{modelRoute}}.destroy', ${{modelView}}->id) }}" method="POST">
    @csrf
    <a class="" href="{{ route('{{modelRoute}}.show', ${{modelView}}->id) }}"><i class="text-dark ph-eye"></i></a>
    <a class="" href="{{ route('{{modelRoute}}.edit', ${{modelView}}->id) }}"><i class="ph-pencil"></i></a>
    @method('DELETE')
    <a type="submit" class="text-danger sa-confirm"><i class="ph-trash"></i></a>
</form>
